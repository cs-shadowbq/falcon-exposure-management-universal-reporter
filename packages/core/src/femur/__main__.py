"""falcon-application-inventory is a library — use the CLI or server package.

Install the CLI::

    pip install falcon-application-inventory-cli

Install the server::

    pip install falcon-application-inventory-server

"""
import sys

print(__doc__, file=sys.stderr)
sys.exit(1)
